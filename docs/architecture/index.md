# Architecture

`wf` is a single binary with a linear execution pipeline and no external runtime dependencies.

- [Architecture Overview](overview.md) — data flow, key packages, storage schema, and design decisions
